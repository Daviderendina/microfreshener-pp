apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: users-service-timeout
  namespace: default
spec:
  http:
  - route:
    - destination:
        host: users.default.svc
    timeout: 2s
  hosts:
  - users.default.svc